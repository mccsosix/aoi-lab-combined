# AOI LAB 子页面设计规范

这份规范用于 AOI LAB 后续所有工具页。目标不是让每个工具长得一模一样，而是让用户一眼知道：它们属于同一个工具箱，也使用相同的操作语言。

## 1. 设计基调

- 视觉关键词：实验室记录、工程图纸、2D 工具面板、清晰、可靠、有一点手作感。
- 不使用拟物 3D、玻璃模糊、发光渐变或大面积圆角。
- 主要层级依靠色块、硬边框、实色偏移阴影和留白建立。
- 装饰不能盖过功能；输入、状态、结果永远优先。

## 2. 设计变量

| 用途 | 变量 | 色值 |
|---|---|---|
| 深色背景 / 主文字 | `--navy` / `--ink` | `#072F50` / `#08304F` |
| 奶油纸张 | `--cream` / `--paper` | `#FFF9E8` / `#FFFAEA` |
| 主强调色 | `--cyan` | `#24D4D3` |
| 浅强调底色 | `--cyan-soft` | `#9CE7DF` |
| 主要操作 / 警示 | `--orange` | `#FF9D2E` |

规则：

- 一个界面最多同时使用一个主强调色和一个操作色。
- 正文使用深蓝，不使用纯黑。
- 阴影是无模糊的实色偏移：小组件 `3px 3px`，卡片 `5px 5px`，页面容器 `8–10px`。
- 默认边框 2px；页面外框和强调卡片可用 3px。

## 3. 字体与间距

- 中文正文：系统无衬线字体。
- 数字、编号、状态和代码标签：`Geist Mono` 或等宽字体。
- 页面标题：32–64px，字重 900；分区标题 20–24px；正文 14–16px；辅助文字不得小于 12px。
- 间距以 4px 为基本单位。常用值：8、12、16、24、32、40、48。
- 同一分区内元素间距 8–24px；分区之间 40–48px。

## 4. 工具页骨架

每个工具页都使用 `ToolPageShell`：

1. 深蓝工程网格背景。
2. 顶部 AOI LAB 标识和面包屑。
3. 奶油色工作纸张。
4. 编号、工具名称、说明和返回按钮。
5. 页面主体。

推荐主体模板：

| 场景 | 桌面布局 | 移动端 |
|---|---|---|
| 参数配置 + 结果预览 | 左 65–72%，右 28–35% | 单列，预览放在配置后 |
| 查询 + 结果列表 | 上方查询区，下方结果区 | 单列 |
| 文件处理流程 | 上传、配置、结果三段纵向 | 单列 |
| 简单单功能 | 居中的最大宽度内容区 | 单列 |

参数很多时，右侧只放摘要、文件名和最终操作，不复制左侧输入控件。

## 5. 组件规则

### 分区标题

- 使用 `01 / 02 / 03` 编号方块。
- 标题描述“用户要完成的动作”，例如“选择评估模板”“设置基础参数”。
- 可加一行简短说明，不写长段落。

### 输入框

- 高度至少 44px，2px 深蓝边框，奶油白背景。
- 标签始终显示，不用 placeholder 代替标签。
- 单位放在输入框旁或输入框内部末端。
- 键盘聚焦时使用 3px 橙色外轮廓。

### 选择卡

- 适合 2–4 个互斥选项。
- 选中状态同时使用：背景色、边框、勾选标记和实色阴影；不能只靠颜色。
- 超过 4 个选项改用下拉框或列表。

### 表格与重复项目

- 桌面端使用列对齐；窄屏可横向滚动或转为卡片。
- 每行操作顺序统一：上移、下移、删除。
- 删除使用红色描边；仅剩一行时禁用删除。
- 新增按钮放在列表下方左侧。

### 主要操作

- 每页只保留一个主要操作，使用橙色底、深蓝边框和实色阴影。
- 按钮文案描述结果，如“生成并下载”，不要只写“确定”。
- 操作后必须显示加载、成功或失败反馈。

### 状态

- 空状态：说明下一步并提供操作。
- 加载：按钮保留宽度，显示处理中并禁用重复提交。
- 成功：明确文件或结果在哪里。
- 错误：靠近出错字段显示原因和修复方法。
- 禁用：降低透明度，同时保留可辨认文字。

## 6. 响应式与可访问性

- 1100px 以下将双栏改为单栏。
- 680px 以下页面外边距为 12px，内容内边距为 20px。
- 所有可点击区域至少 44×44px。
- 所有输入都有可读标签；图标按钮必须有 `aria-label`。
- 不用颜色作为唯一状态提示。
- 支持键盘导航、清晰焦点和 `prefers-reduced-motion`。
- 表格横向滚动时，不允许整个页面产生横向滚动。

## 7. 代码组织

```text
app/
  components/
    tool-page-shell.tsx
  tools/
    sheet-generator/
      page.tsx
    new-tool/
      page.tsx
```

- 新工具统一放在 `app/tools/<slug>/page.tsx`。
- 复用页面骨架，不复制 `ToolPageShell`。
- 每个工具负责自己的状态和业务逻辑。
- 主页卡片只负责导航，不承载工具业务。
- 页面样式使用清晰前缀，避免与主页全局样式冲突。

## 8. 新增工具检查清单

- [ ] 分配两位编号和稳定的英文 slug。
- [ ] 在主页添加卡片并链接到 `/tools/<slug>`。
- [ ] 使用 `ToolPageShell`，写清工具名和一句话说明。
- [ ] 选择合适的主体模板。
- [ ] 补齐空、加载、成功、错误状态。
- [ ] 在 1440px、1024px、390px 宽度检查布局。
- [ ] 用键盘完成主要流程。
- [ ] 添加页面渲染测试和关键交互测试。
- [ ] 更新 README 的功能列表。
- [ ] 执行 `npm run lint` 与 `npm test`。

