# AOI LAB 前端设计准则

## 1. 设计定位

AOI LAB 是个人维护的小工具实验室，不是传统企业后台，也不是工业检测软件界面。

网站的视觉关键词：

- 纯 2D
- 独立开发者实验室
- 工程网格
- 手绘线条
- 便签与胶带
- 清晰、轻松、实用
- 持续生长的小工具集合

新增页面必须让用户感受到：这是同一个人在同一个实验室里做出来的工具。

## 2. 必须保持的原则

### 应该做

- 保持深蓝工程网格背景。
- 使用米白色纸张作为主要内容容器。
- 使用青蓝色表达主要操作、选中状态和工具标签。
- 使用橙色表达提醒、生成、重点和少量视觉惊喜。
- 图标采用二维线稿，线条圆润，允许轻微不规则。
- 卡片以边框、错位色块和少量旋转表现层次。
- 页面内容优先清晰可用，装饰只服务于氛围。
- 所有交互必须同时提供鼠标悬停和键盘焦点状态。

### 不应该做

- 不使用伪 3D、拟物金属、玻璃拟态或写实材质。
- 不使用大面积渐变、霓虹光晕和科幻 HUD。
- 不做成常见的白色企业管理后台。
- 不使用过多圆角卡片，主要卡片保持接近纸张的直角外形。
- 不让便签、箭头和涂鸦影响信息阅读。
- 不为不同工具随意更换主色和字体。

## 3. 颜色系统

颜色变量以 `app/globals.css` 中的 CSS 自定义属性为唯一来源。

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--navy` | `#072F50` | 页面主背景 |
| `--cream` | `#FFF9E8` | 深色背景上的主要文字、图标 |
| `--paper` | `#FFFAEA` | 工具卡、表单和主要内容容器 |
| `--cyan` | `#24D4D3` | 主操作、选中状态、强调线条 |
| `--cyan-soft` | `#9CE7DF` | 标签、柔和选中区域 |
| `--orange` | `#FF9D2E` | 生成、提醒、局部重点 |
| `--ink` | `#08304F` | 浅色纸张上的正文与边框 |

### 使用比例

- 深蓝背景：约 55%。
- 米白纸张：约 30%。
- 青蓝强调：约 10%。
- 橙色强调：不超过 5%。

不要为单个新页面新增相似颜色。如果确实需要状态色，应先定义语义：

- 成功：可复用 `--cyan`。
- 警告：使用 `--orange`。
- 错误：可新增克制的砖红色，但必须补充到本规范和全局变量。

## 4. 字体与文字层级

### 字体

- 中文正文：`PingFang SC`、`Microsoft YaHei` 或系统无衬线字体。
- 英文标题、编号、状态和代码感标签：`Geist Mono`。
- 不引入装饰性手写字体。手绘感由排版、旋转和线条实现。

### 推荐字号

| 类型 | 桌面端 | 移动端 |
| --- | --- | --- |
| 品牌标题 | `56–76px` | `32–48px` |
| 页面主标题 | `44–72px` | `36–56px` |
| 工具标题 | `28–44px` | `26–36px` |
| 区块标题 | `22–30px` | `20–26px` |
| 正文 | `15–17px` | `14–16px` |
| 标签/状态 | `12–14px` | `12–13px` |

### 文案语气

- 简短、直接、略带个人感。
- 优先说明工具解决什么问题。
- 避免“赋能、生态、闭环、智能化平台”等企业套话。
- 推荐格式：动作 + 结果，例如“上传表格，快速找到对应物件”。

## 5. 网格、尺寸与间距

### 页面宽度

- 桌面最大内容宽度：`1480px`。
- 页面左右安全间距：桌面 `28px` 以上，移动端 `14px` 以上。
- 主要断点：`980px` 和 `680px`。

### 间距基准

采用 4px 倍数体系：

- 极小间距：`4px / 8px`
- 组件内部：`12px / 16px / 24px`
- 卡片间距：`28px`
- 大区块间距：`40px / 48px / 64px`

新页面不要混用大量无规律数值。

## 6. 核心组件规则

### 工具卡片

完整工具卡应包含：

1. 工具编号。
2. 二维线稿图标。
3. 工具名称。
4. 一句话用途。
5. 1–3 个标签。
6. 明确的打开入口。

视觉规则：

- 背景使用 `--paper`。
- 主边框使用 `3px solid --ink`。
- 默认错位阴影使用青蓝色实心块，不使用模糊阴影。
- 悬停时轻微上移，并将错位色切换为橙色。
- 旋转角度控制在 `±1deg` 以内。

### 表单和数据页面

工具内部页面应延续“工程纸张”感觉：

- 表单容器使用米白纸张。
- 输入框使用深蓝边框，聚焦时使用青蓝边框或外圈。
- 主按钮使用青蓝底和深蓝文字。
- 次按钮使用透明底和米白/深蓝边框。
- 危险操作不能使用青蓝色，应使用明确的错误色。
- 表格表头可用浅青色，行分隔使用低透明度深蓝线。
- 空状态使用二维线稿图标和一句可执行建议。

### 便签与涂鸦

- 每个首屏最多使用 2–3 个显眼装饰。
- 便签用于补充语气，不承载关键操作。
- 箭头只用于引导用户看到主要操作。
- 装饰元素默认设置 `aria-hidden="true"`。

### 弹窗

- 优先使用居中的纸张式面板或右侧抽屉。
- 遮罩使用深蓝色半透明，不使用毛玻璃。
- 弹窗必须包含可见标题、关闭按钮和键盘焦点管理。

## 7. 新增工具页面模板

新增工具建议使用以下结构：

1. 返回首页。
2. 工具编号与分类标签。
3. 工具名称和一句话说明。
4. 输入区。
5. 主操作区。
6. 结果区。
7. 错误、空状态或处理说明。

页面布局建议：

```text
顶部：AOI LAB / 返回工具箱

工具介绍
├─ 编号与标签
├─ 标题
└─ 简短说明

工作区
├─ 左侧：输入与参数
└─ 右侧：预览或结果

底部：操作说明 / 最近更新
```

移动端必须自动改为单列，结果区位于输入区之后。

## 8. 图标规范

- 优先使用内联 SVG，方便颜色继承和响应式缩放。
- `viewBox` 必须存在。
- 主要线宽保持视觉一致，推荐 `3–5px`。
- 使用 `stroke-linecap="round"` 和 `stroke-linejoin="round"`。
- 图标只使用 `--ink`、`--cyan`、`--cyan-soft`、`--orange`。
- 功能图标需要表达动作，而不仅是文件类型。
- 装饰图标添加 `aria-hidden="true"`；独立操作图标必须提供文字或 `aria-label`。

## 9. 交互与动效

- 常规过渡：`160–200ms`。
- 卡片悬停位移：最多 `4px`。
- 不使用长时间循环动画。
- 不使用大幅缩放、旋转或弹跳。
- 键盘 `focus-visible` 的清晰度不得低于鼠标悬停状态。
- 必须保留 `prefers-reduced-motion` 处理。
- 所有点击区域建议不小于 `44×44px`。

## 10. 响应式规则

### 桌面端：大于 980px

- 首页主要工具双列展示。
- 未来工具位于侧栏。
- 工具内部页面可以使用双栏工作区。

### 平板：681–980px

- 主工具仍可双列，但未来工具移动到下方。
- 减少装饰，保证卡片文字不拥挤。

### 手机：不超过 680px

- 所有主要内容改为单列。
- 隐藏非必要便签和涂鸦。
- 主标题左对齐。
- 按钮可以占满容器宽度。
- 表格较宽时提供横向滚动，不压缩到无法阅读。

## 11. 可访问性

- 正文和背景对比度至少满足 WCAG AA。
- 不只依靠颜色表达状态，应同时使用文字或图标。
- 每个表单控件必须有关联标签。
- 错误信息要和对应字段建立关系。
- 链接和按钮必须可以使用键盘操作。
- 纯装饰 SVG、字符和图形需要隐藏于辅助技术。
- 页面标题层级从 `h1` 开始，不跳级。

## 12. Agent 修改规则

本地 Agent 在新增或修改界面时必须：

1. 先阅读本文件。
2. 先复用现有 CSS Token，再考虑新增 Token。
3. 优先扩展现有组件，不复制粘贴大型页面。
4. 新工具功能代码放到独立路由和独立模块。
5. 不把 Excel 解析、文件生成等业务逻辑写进首页组件。
6. 修改后运行：

```bash
npm run lint
npm test
```

7. 检查桌面端和移动端布局。
8. 如果确实修改设计基调，必须同步更新本文件。

## 13. 提交前 UI 自检

- [ ] 是否仍然是纯 2D 风格？
- [ ] 是否只使用规定的主要颜色？
- [ ] 新页面是否像 AOI LAB，而不是普通后台模板？
- [ ] 主要操作是否足够明显？
- [ ] 是否存在清晰的悬停和键盘焦点状态？
- [ ] 手机端是否为单列且无横向溢出？
- [ ] 装饰是否没有遮挡文字和操作？
- [ ] 空状态、错误状态和加载状态是否齐全？
- [ ] 是否通过代码检查和页面测试？
